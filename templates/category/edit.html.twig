{% extends 'base.html.twig' %}

{% block title %}Modification d'une catégorie{% endblock %}

{%  block body %}
    <p class="h1">Modifier une catégorie</p>
    {{ form_start(form) }}
    {{ form_widget(form) }}

    <button type="submit" class="btn btn-success float-right">Modifier !</button>

    {{ form_end(form) }}
{% endblock %}